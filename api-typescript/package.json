{
  "name": "api-typescript",
  "version": "1.0.0",
  "description": "npm run tsc -- --init (se ponen 2 guines antes para que no lo tome como parte del npm)\r npm i typescript -d -E(para la version exacta)",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"tsc -w\" \"nodemon dist/app.js\"",
    "start": "node dist/app.js",
    "tsc": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.14",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "concurrently": "^7.5.0",
    "express": "^4.18.2",
    "nodemon": "^2.0.20"
  }
}
